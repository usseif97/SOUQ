import 'package:flutter/material.dart';

class SettingsSvreen extends StatelessWidget {
  const SettingsSvreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DATA'),
      ),
    );
  }
}
